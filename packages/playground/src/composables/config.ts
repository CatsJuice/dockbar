export const config = reactive({
  allowDragDelete: false,
  disabled: false,
  direction: 'horizontal',
  size: 40,
  padding: 8,
  gap: 8,
  maxScale: 2,
  maxRange: 200,
  position: 'bottom',
  sortable: true,
})
